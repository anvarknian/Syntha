apiVersion: apps/v1
kind: Deployment
metadata:
  name: fake-salesforce
  labels:
    app.kubernetes.io/name: fake-salesforce
    app.kubernetes.io/component: fake-saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fake-salesforce
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fake-salesforce
        app.kubernetes.io/component: fake-saas
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: fake-salesforce
          image: syntha/fake-salesforce:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: syntha-config
          env:
            - name: PORT
              value: "3004"
            - name: EXPORT_DENY_FIELDS
              value: description
            - name: HOME
              value: /tmp
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
          ports:
            - name: http
              containerPort: 3004
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: data
          emptyDir: {}
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: fake-salesforce
  labels:
    app.kubernetes.io/name: fake-salesforce
spec:
  selector:
    app.kubernetes.io/name: fake-salesforce
  ports:
    - name: http
      port: 3004
      targetPort: http
